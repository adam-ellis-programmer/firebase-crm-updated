import React from 'react'
import { useEffect, useState } from 'react'
import { getAgentsCustomers } from '../../crm context/CrmAction'

const DirectSubordinates = () => {
  // get all data where id
  // is equal to every id in the
  // viewing agents subordinate arr
  return (
    <div>
      <h1>direct subordinates</h1>
    </div>
  )
}

export default DirectSubordinates
